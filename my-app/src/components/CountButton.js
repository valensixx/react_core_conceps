import React from "react";

const CountButton = () => {

    const currtntCount = 0;

    return (
        <div>
            <button type="">+1</button>
            <div>{currtntCount}</div>
        </div>
    )
    
}

export default CountButton;